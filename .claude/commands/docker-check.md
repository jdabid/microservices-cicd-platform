Validate all Docker configurations. Read all Dockerfiles and docker-compose files.

Check: multi-stage builds, non-root user, HEALTHCHECK, --no-cache-dir, slim base, layer caching, no secrets, health checks in compose, depends_on conditions, named volumes, network isolation.

Try building images and present results as PASS/FAIL checklist.
